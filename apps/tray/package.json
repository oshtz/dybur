{
  "name": "@dybur/tray",
  "version": "1.3.4",
  "private": true,
  "description": "dybur tray application",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "prebuild": "node -e \"require('fs').cpSync('../../packages/cli/dist/cli.js', 'src-tauri/resources/cli.js'); console.log('CLI synced to resources')\"",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@dybur/cli": "workspace:*",
    "@tauri-apps/api": "^2.0.0"
  }
}
