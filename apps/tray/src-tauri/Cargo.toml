[package]
name = "dybur-app"
version = "1.2.0"
description = "dybur - Local voice dictation"
authors = ["Omer Shatzberg <omershatzberg@gmail.com>"]
license = "MIT"
repository = "https://github.com/oshtz/dybur"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon", "macos-private-api"] }
tauri-plugin-shell = "2"
tauri-plugin-global-shortcut = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "5"
ureq = { version = "2.9", features = ["tls"] }
cpal = "0.15"
rodio = "0.18"
chrono = "0.4"
lazy_static = "1.4"

# ndarray for tensor operations (ort is platform-specific below for GPU support)
ndarray = "0.17"

# Audio processing for mel spectrograms
rustfft = "6.2"

# Windows-specific dependencies for native keyboard/clipboard + GPU
[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = [
    "Win32_Foundation",
    "Win32_System_DataExchange",
    "Win32_System_Memory",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_UI_WindowsAndMessaging",
    "Win32_Globalization",
    "Win32_Graphics_Dwm",
    "Win32_Graphics_Gdi"
] }
# ONNX Runtime with DirectML for GPU acceleration on Windows
ort = { version = "2.0.0-rc.11", default-features = false, features = [
    "std", "ndarray", "download-binaries", "copy-dylibs", "tls-native",
    "directml"
] }

# macOS-specific: ONNX Runtime with CoreML for GPU/Neural Engine acceleration
[target.'cfg(target_os = "macos")'.dependencies]
ort = { version = "2.0.0-rc.11", default-features = false, features = [
    "std", "ndarray", "download-binaries", "copy-dylibs", "tls-native",
    "coreml"
] }

# Other platforms (Linux, etc.): CPU-only ONNX Runtime
[target.'cfg(not(any(windows, target_os = "macos")))'.dependencies]
ort = { version = "2.0.0-rc.11", default-features = false, features = [
    "std", "ndarray", "download-binaries", "copy-dylibs", "tls-native"
] }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
